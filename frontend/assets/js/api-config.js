/* Адрес API Нейро-сомелье.
   Прод: пустая строка — nginx проксирует /api на бэкенд с того же домена.
   Локально (localhost/127.0.0.1): main.js сам подставит http://127.0.0.1:8080.
   Если API живёт отдельно — укажите полный URL своего API. */
window.VINOTERRA_API_URL = "";
