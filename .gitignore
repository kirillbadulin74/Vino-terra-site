# Секреты
.env
*.key

# Системное
Thumbs.db
.DS_Store
__pycache__/

# Рабочее
_cmp/
_shots/
frontend/assets/img/_orig_1254_backup/
HANDOFF.md
feedback/
.claude/settings.local.json

# Архивы для переноса на сервер
*.tar.gz

# Производный кеш эмбеддингов — собирается локально и на сервере
# (backend/build_vector_index.py), в Git не хранится.
backend/vector_index/

# Конфигурация живого сервера — конфиденциально, репозиторий публичный.
# Хранится только локально (см. приватный отчёт в Diploma/reports/).
deploy/nginx-*server*.conf
